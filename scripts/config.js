/**
 * White listed websites for which it is safe to apply the simple flat menu strategy
 * to click buttons, such as "Reject all". On those sites we can click those buttons
 * with high confidence.
 */
const WHITELISTED_DOMAINS_FOR_SIMPLE_FLAT_MENUS = [
    "google",
    "amazon",
];